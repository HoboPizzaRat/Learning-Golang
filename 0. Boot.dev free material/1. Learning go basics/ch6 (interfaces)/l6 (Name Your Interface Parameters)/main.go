package main

// its better to name your interface parameters

/*
type Copier interface {
  Copy(sourceFile string, destinationFile string) (bytesCopied int)
}
*/

// this way we know what paremeters it can accept and its more self documenting
