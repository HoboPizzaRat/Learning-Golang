package main

// The go build command compiles go code into a single, statically linked executable program.
//		- the output is a statically compiled binary, you can ship it to
//		production or end users without them needing the Go toolchain installed.

// ome new Go devs use go run on a server in production, which is a huge mistake.
