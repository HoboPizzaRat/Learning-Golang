This is very peculiar

Byte is an alias for uint8.
- it is legal to do math operations between byte and uint8
- just use byte name convention whenever preferred

Int is on 32-bit cpu just int32, and 64-bit cpus its int64.
- it isn't consistent from platform to platform

uint is for unsigned values.
- non negative so to say


## so where you use which integer
If you are working with a binary file format or network protocol that has an inte‐
ger of a specific size or sign, use the corresponding integer type.
- for example in your emulator project you would benefit from that

On any other case where you are not constrained by space, just use int64

