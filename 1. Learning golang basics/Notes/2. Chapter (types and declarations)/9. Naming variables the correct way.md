Even though underscore is a valid character in a variable name, it is rarely used,
- THE GO USES CAMEL CASE NOT SNAKE CASE


so name your variables like so:
```go
var superCoolVariable = "kekkonen"
fmt.Prinln(superCoolVariable)

```