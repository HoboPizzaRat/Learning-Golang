Go is statically typed language
- built in types and user defined types
- it has type abstraction (you can write code that invokes methods without specifying implementation explicitly)

Go is designed to encourage the best practices that are advocated by software engineers.

