Go developers have adopted make as their solution to specify build steps

One drawback to Makefiles is that they are exceedingly picky. You must indent the
steps in a target with a tab. They are also not supported out-of-the-box on Windows

best way is to use chocolatey on windows machines to install make
```powershell
choco install make
```