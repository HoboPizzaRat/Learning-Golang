they make it harder to understand data flow and can create extra work for the garbage collector

```go
// dont do this
func MakeFoo(f *Foo) error {
	f.Field1 = "val"
	f.Field2 = 20
	return nil
}
// Do this
func MakeFoo() (Foo, error) {
	f := Foo{
		Field1: "val",
		Field2: 20,
	}
	return f, nil
}

```

we’ll see that with
buffers for reading or writing data. In addition, there are data types that are used with
concurrency that must always be passed as pointers. 

If a struct is large enough, there are performance improvements from using a pointer to the struct or arrays or whatever.
- Passing a value into a function takes longer as the data gets larger.