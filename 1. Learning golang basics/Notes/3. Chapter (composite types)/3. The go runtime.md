The Go runtime provides services like
memory allocation and garbage collection, concurrency support, networking, and
implementations of built-in types and functions

The go runtime itself is something that is baked into every go binary.
- its not a virtual machine in a sense cuz it runs on baremetal code.
	- shipping runtime is common with virtual machine based languages(jave)
	- 