You have already seen how to declare the functions and how to call them
- mainly the main function and the fmt method calling

example of a function with return value
```go
func div(numerator int, denominator int) int {
	if denominator == 0 {
		return 0
	}
	return numerator / denominator
}

```