blank returns are no go in go!

If you have named return values you can write return without specifying specifying the returnable values.

if you don't specify return values you will return their return values in case of error

It can become quite weird if you catch returned values from blank returns and still put them into variables. 
- invalid input may be still accepted and processed etc
