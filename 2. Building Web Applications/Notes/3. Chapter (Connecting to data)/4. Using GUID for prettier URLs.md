switching to the alphanumeric GUID is trivial.

```go
routes.HandleFunc("/page/{id:[0-9a-zA\\-]+}", ServePage)
```

we only need to change the regex and....

The page will still pass through our handler
we change the above expression just a little more
```go
routes.HandleFunc("/page/{guid:[0-9a-zA\\-]+}", ServePage)
```