If we use the following, we must always restart the application every time the template needs to change.
```go
tpl, err := template.New("mine").Parse(`<h1>{{.Title}}</h1>`)
```

The first thing we need to do to move in application html strings to file based templates is to create a template file.
