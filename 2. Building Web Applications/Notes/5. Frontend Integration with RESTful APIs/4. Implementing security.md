In our case, read data is totally benign. If someone can access all of our blog entries
via a GET request, who cares?

Ok so lets tryna make our api private by the following manner
- we need to make sure that only the authorized users can make delete and modifying requests to the pages
- ok how we do that

We can provide API tokens that will allow short-lived request windows, which can
be validated by a shared secret. 
- Without the shared secret, the request and its token will never match, and an API request can then be rejected.
- if we allow cleartext api keys, 

Go provides a very easy way to utilize either HTTP or
HTTPS via Transport Layer Security (TLS); TLS is the successor of SSL